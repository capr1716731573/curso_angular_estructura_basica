import { ConvertidorPipe } from './convertidor.pipe';

describe('ConvertidorPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertidorPipe();
    expect(pipe).toBeTruthy();
  });
});
